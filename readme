# AIDC 上位机打包说明

## 打包环境

- Windows
- Python 环境：`C:\Users\ch\.conda\envs\QT`
- 入口文件：`main.py`
- 打包配置：`main.spec`
- CAN 驱动 DLL：`ControlCAN.dll`

## 打包前检查

在项目根目录执行：

```powershell
cd D:\DDSAVE\工作\AIDCSWJ
& 'C:\Users\ch\.conda\envs\QT\python.exe' scripts\release_check.py
```

看到 `RELEASE_CHECK_OK` 后再打包。

## 生成 exe

```powershell
cd D:\DDSAVE\工作\AIDCSWJ
& 'C:\Users\ch\.conda\envs\QT\python.exe' -m PyInstaller --clean --noconfirm main.spec
```

输出文件：

```text
D:\DDSAVE\工作\AIDCSWJ\dist\main.exe
```

## 复制运行配置

`config.json` 不会被 `main.spec` 打进 exe。需要把当前运行配置复制到 exe 同目录：

```powershell
Copy-Item -LiteralPath config.json -Destination dist\config.json -Force
```

`main.spec` 已经包含：

- `conf.yaml`
- `ControlCAN.dll`
- `alarm.wav`
- `IDC.dbc`
- `UI\release_theme.qss`
- `UI\style.qss`
- `resources\index_catalog.json`
- `resources\index_custom_template.yaml`

## 索引浏览器与自定义配置

在“主机控制”页面点击“浏览索引”，可按中文名称、C 符号名、十进制索引或十六进制索引搜索。选中后可直接填入当前请求行。

浏览器支持导入 JSON/YAML 自定义配置，可覆盖名称、描述、有符号类型、比例、偏移、单位、访问权限和枚举映射，也可增加自定义索引。默认新增项为只读，需要写入时必须显式设置 `access: read_write`。

导入后的有效配置保存在 exe 同目录：

```text
D:\DDSAVE\工作\AIDCSWJ\dist\index_custom.yaml
```

可从浏览器点击“导出模板”获得完整字段示例；点击“清除自定义”可恢复使用内置的固件索引目录。

## 告警参数

“告警参数”页面默认快速读取启用告警的阈值和回差摘要。选中列表项后自动优先读取该告警的 30 个有效字段，读取完成后才允许编辑写入。

写入只发送实际变化字段。下位机逐项应答后，上位机会自动回读并校验；超时或校验失败时会列出字段编号。701 固件仅在工装模式或模拟测试模式下接受参数写入。

## 上下电诊断

“上下电诊断”页面基于 `D:\DDSAVE\工作\IDC\下位机\701\01.bcu_app_01v01` 的电池管理状态机实时分析：

- 无法上电：展示任务就绪、严重告警、电流传感器、绝缘检测、继电器自检、断路器、编址、VMS请求和预充条件。
- 异常下电：记录高压运行状态的迁移，关联严重告警、断路器、继电器异常断开、高压采样、电流传感器和HVIL证据。
- 带中线时，上半簇和下半簇的严重告警、总压、母线电压及VMS继电器请求分开判断。
- 页面支持自动刷新、立即刷新和JSON诊断快照导出。
- 新出现上电阻断或异常下电时，全窗口红色闪烁三轮；同一持续故障只提示一次，恢复后再次出现才重新提示。

## 启动验证

```powershell
cd D:\DDSAVE\工作\AIDCSWJ
$exe = Join-Path (Get-Location) 'dist\main.exe'
$work = Join-Path (Get-Location) 'dist'
$p = Start-Process -FilePath $exe -WorkingDirectory $work -PassThru -WindowStyle Hidden
Start-Sleep -Seconds 5
if ($p.HasExited) { Write-Output "启动失败: $($p.ExitCode)" } else { Stop-Process -Id $p.Id -Force; Write-Output "启动成功" }
```

## 日志目录

运行 exe 后，日志默认保存到 exe 同目录下：

```text
D:\DDSAVE\工作\AIDCSWJ\dist\hisData
```

源码方式运行时，日志保存到项目根目录：

```text
D:\DDSAVE\工作\AIDCSWJ\hisData
```

## 页面刷新优先级

- 当前可见的簇概览、BAU、实时监控、均衡、电芯异常和上下电诊断页面使用高优先级索引请求。
- 单体电压、单体温度、均衡状态和单体3D页面持续缓存数据，仅当前页面执行高频绘制。
- 总压和霍尔/分流器电流保持全局共享缓存，K线页面可见时自动提高采样频率，不重复发送同一索引。
- CAN链路无下位机响应时仍自动降频，避免未连接状态占满界面线程。
